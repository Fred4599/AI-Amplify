---
import { BOOKING_URL } from '../config';
---
<section id="join" class="w-full max-w-[1200px] px-4 sm:px-6 py-16 sm:py-24 md:py-32 z-20 scroll-mt-24">
    <div class="glass-panel rounded-2xl sm:rounded-3xl p-6 sm:p-12 md:p-16 lg:p-20 relative overflow-hidden shadow-2xl shadow-purple-900/20">
        <div class="absolute inset-0 bg-[url('https://grainy-gradients.vercel.app/noise.svg')] opacity-10 mix-blend-overlay"></div>
        <div class="absolute bottom-0 left-1/2 -translate-x-1/2 w-[600px] h-[300px] bg-purple-500/10 blur-[100px] rounded-full"></div>
        <div class="absolute top-0 right-0 w-[400px] h-[400px] bg-purple-600/15 blur-[100px] rounded-full"></div>

        <div class="relative z-10 flex flex-col items-center max-w-xl mx-auto">
            <!-- Section label -->
            <span class="reveal-text text-xs font-mono text-purple-400 uppercase tracking-widest mb-3 block">Free Strategy Call</span>

            <h2 class="reveal-text font-display text-2xl sm:text-4xl md:text-5xl font-bold tracking-tight text-white text-center mb-6">
                Ready to Start Making Money with AI?
            </h2>

            <p class="reveal-text text-neutral-400 text-center text-sm sm:text-base leading-relaxed mb-6">
                Apply for your free 15-minute call. A few quick questions (60 seconds), then pick a time. On the call you get a personalized roadmap to your first $1K–5K with AI—no risk, no obligation.
            </p>

            <p class="reveal-text text-white/90 text-center text-sm font-medium mb-8">
                Spots are limited. If you're serious, book now.
            </p>

            <!-- CTA block -->
            <div class="reveal-text w-full flex flex-col items-center mb-8">
                <a href={BOOKING_URL} target="_blank" rel="noopener noreferrer" class="group w-full sm:w-auto max-w-md inline-flex items-center justify-center gap-2 px-8 py-5 sm:py-6 bg-white text-black rounded-2xl font-bold text-base sm:text-lg hover:bg-neutral-200 hover:scale-[1.02] active:scale-[0.98] transition-all shadow-[0_0_24px_rgba(255,255,255,0.25)]">
                    Book My Free AI Money-Making Strategy Call
                    <i data-lucide="arrow-right" class="w-5 h-5 shrink-0 transition-transform group-hover:translate-x-0.5"></i>
                </a>
                <p class="text-neutral-500 text-xs sm:text-sm mt-3 text-center">
                    Only 12 spots left this week · 60 seconds to apply · 100% free
                </p>
            </div>

            <!-- Trust badges: compact strip -->
            <div class="reveal-text w-full rounded-xl bg-white/[0.03] border border-white/5 p-4 sm:p-5">
                <div class="grid grid-cols-2 gap-3 sm:gap-4">
                    <span class="inline-flex items-center gap-2 text-neutral-400 text-xs sm:text-sm"><i data-lucide="check-circle" class="w-4 h-4 text-purple-400 shrink-0"></i> No Credit Card Required</span>
                    <span class="inline-flex items-center gap-2 text-neutral-400 text-xs sm:text-sm"><i data-lucide="check-circle" class="w-4 h-4 text-purple-400 shrink-0"></i> No Obligation to Buy</span>
                    <span class="inline-flex items-center gap-2 text-neutral-400 text-xs sm:text-sm"><i data-lucide="check-circle" class="w-4 h-4 text-purple-400 shrink-0"></i> 100% Free Strategy Session</span>
                    <span class="inline-flex items-center gap-2 text-neutral-400 text-xs sm:text-sm"><i data-lucide="check-circle" class="w-4 h-4 text-purple-400 shrink-0"></i> Personalized Action Plan</span>
                </div>
            </div>
        </div>
    </div>
</section>

<script>
    const observer = new IntersectionObserver((entries) => {
        entries.forEach(entry => {
            if (entry.isIntersecting) {
                entry.target.classList.add('reveal-visible');
            }
        });
    }, { threshold: 0.1 });
    document.querySelectorAll('#join .reveal-text').forEach(el => observer.observe(el));
</script>
